apiVersion: apps/v1
kind: Deployment
metadata:
  name: evaluation
  namespace: togglemaster
  labels:
    app: evaluation
spec:
  replicas: 2
  selector:
    matchLabels:
      app: evaluation
  template:
    metadata:
      labels:
        app: evaluation
    spec:
      serviceAccountName: evaluation-sa
      containers:
        - name: evaluation
          image: <ECR_REGISTRY>/togglemaster/evaluation:v0.0.1-init
          ports:
            - containerPort: 8080
          envFrom:
            - configMapRef:
                name: evaluation-config
          env:
            - name: PORT
              value: "8080"
          readinessProbe:
            httpGet: { path: /healthz, port: 8080 }
            initialDelaySeconds: 5
          livenessProbe:
            httpGet: { path: /healthz, port: 8080 }
            initialDelaySeconds: 10
          resources:
            requests: { cpu: 50m, memory: 64Mi }
            limits: { cpu: 250m, memory: 128Mi }
          securityContext:
            runAsNonRoot: true
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
